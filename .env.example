NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
ADMIN_PASSWORD=changeme

# Booking token signing secrets (generate secure 32-byte hex values)
# Checkout token secret (30 minute expiry tokens)
BOOKING_CHECKOUT_TOKEN_SECRET=replace-with-64-char-hex-secret
# Booking confirmation token secret (24 hour expiry tokens)
BOOKING_CONFIRMATION_SECRET=replace-with-64-char-hex-secret
